[project]
name = "iterecho"
dynamic = ["version"]
description = "Secure file processor with copy, concatenate and chunk modes"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"
authors = [
    { name = "morphilab", email = "lab@morphilab.com" },
]
keywords = [
    "file-processing",
    "cli",
    "security",
    "concatenation",
    "file-copy",
    "file-chunk",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Utilities",
    "Topic :: System :: Filesystems",
    "Topic :: Security",
]
dependencies = [
    "tqdm>=4.66",
    "typer[all]>=0.12",
]

[project.scripts]
iterecho = "iterecho.cli:app"

[project.urls]
Homepage = "https://github.com/Morphilab/iterecho"
Repository = "https://github.com/Morphilab/iterecho.git"
"Bug Tracker" = "https://github.com/Morphilab/iterecho/issues"
Changelog = "https://github.com/Morphilab/iterecho/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov",
    "black>=22.1.0",
    "isort",
    "ruff",
    "mypy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["iterecho"]

[tool.hatch.build.targets.sdist]
include = [
    "iterecho",
    "tests",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "CONTRIBUTING.md",
    "CODE_OF_CONDUCT.md",
    "SECURITY.md",
    "pyproject.toml",
]

[tool.hatch.version]
path = "iterecho/__init__.py"

[tool.black]
line-length = 100
target-version = ["py38", "py39", "py310", "py311", "py312"]

[tool.isort]
profile = "black"
line_length = 100

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP"]
ignore = ["E501", "B008"]   # B008 is normal for Typer defaults

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["-ra", "--strict-markers"]
